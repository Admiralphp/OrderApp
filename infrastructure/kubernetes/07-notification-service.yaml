apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: orderapp
  labels:
    app: notification-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
        tier: backend
    spec:
      containers:
      - name: notification-service
        image: orderapp/notification-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3005
          name: http
        envFrom:
        - configMapRef:
            name: notification-service-config
        - secretRef:
            name: jwt-secret
        - secretRef:
            name: notification-redis-secret
        - secretRef:
            name: smtp-secret
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health/live
            port: 3005
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 3005
          initialDelaySeconds: 15
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: orderapp
  labels:
    app: notification-service
spec:
  type: ClusterIP
  ports:
  - port: 3005
    targetPort: 3005
    protocol: TCP
    name: http
  selector:
    app: notification-service
